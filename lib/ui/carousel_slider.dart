import 'package:flutter/material.dart';

class CarouseSlider extends StatelessWidget {
  const CarouseSlider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [],
    );
  }
}
